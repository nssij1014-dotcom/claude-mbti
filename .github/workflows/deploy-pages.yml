name: GitHub Pages Deployment

# API 라우트/DB 기반 라우트를 제외한 static export만 배포합니다.
# 결과 저장/공유 OG/구글 로그인 등 핵심 기능은 이 배포본에서 동작하지 않습니다.
# Vercel 자동배포는 제거했고(2026-09-02), 현재 이 정적 배포가 유일한 배포 파이프라인입니다.
on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Remove routes unsupported by static export
        run: node scripts/prepare-pages-export.mjs

      - name: Build static export
        env:
          GITHUB_PAGES: "true"
          NEXT_PUBLIC_SITE_URL: https://nssij1014-dotcom.github.io/claude-mbti
        run: npm run build

      - name: Disable Jekyll processing
        run: touch out/.nojekyll

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
